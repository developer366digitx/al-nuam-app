import { Text } from "native-base";
import React from "react";

const Profile = ({ navigation }) => {

    return (
        <Text adjustsFontSizeToFit={true}>Profile</Text>
    )
}

export default Profile;